# Copiar como .env y completar con los valores reales del proyecto Supabase.
# .env está en .gitignore — nunca commitear valores reales.

# --- Supabase (proyecto "Stellar Odyssey") ---
SUPABASE_URL=https://qqpozotcrxfukkwcoget.supabase.co
SUPABASE_ANON_KEY=
# Solo para scripts de servidor/seed — nunca exponer al cliente ni commitear el valor real.
SUPABASE_SERVICE_ROLE_KEY=

# --- Frontend Vite (clave pública únicamente) ---
VITE_SUPABASE_URL=https://qqpozotcrxfukkwcoget.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Registro/login es exclusivamente email/contraseña (sin login social) — no hay credenciales
# de terceros que configurar.
